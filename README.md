# 上班回血器（PWA）

复古小票风移动端网页工具：输入月薪和今日上班时长，实时计算“今天回了多少钱血”。

## 文件说明

- `index.html`：页面结构
- `style.css`：复古小票视觉样式
- `app.js`：计算逻辑 + Service Worker 注册
- `manifest.json`：PWA 清单
- `sw.js`：离线缓存

## 使用方式

直接用静态服务器打开项目根目录即可（例如 `python -m http.server`）。
